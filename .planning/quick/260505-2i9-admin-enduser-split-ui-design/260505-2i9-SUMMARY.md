---
id: 260505-2i9
slug: admin-enduser-split-ui-design
status: complete
date: 2026-05-05
---

# Quick Task 260505-2i9 — Summary

## 任务

将管理端（平台/开发者）和用户端（EndUser）设计为两套独立 UI，明确边界，落库为正式架构决策。

## 完成内容

### Task 1: ai-metadata/prd/enduser-v2/10-redesign-overview.md

在"核心设计原则"章节后新增 `## UI 架构决策` 章节，内容包括：
- 管理端 vs 用户端对比表（入口、受众、功能、路由前缀、特征）
- 明确说明为什么不用一套 UI（维度对比表）
- 说明唯一共享层是 Design System

### Task 2: ai-metadata/prd/enduser-v2/14-frontend-design.md

在文档开头新增 `## 双 UI 架构总览` 章节，包括：
- 路由前缀隔离表（`/org/` vs `/u/`）
- 明确本文档变更范围（管理端路由不变，EndUser v2 变更在用户端）
- 强调两套 UI 的隔离原则

### Task 3: .planning/PROJECT.md

1. Key Decisions 表新增：`双 UI 架构（管理端 /org/ vs 用户端 /u/）`，含决策理由
2. WORKSPACE-01~04 需求条目补充 `**用户端 UI**` 标注，路由更新为 `/u/` 前缀（而非原来的 `/end-user/` 或 `/workspace/`）

## 结论

架构决策已落库。此后开发者可以清晰区分：
- 需要改管理端 → 看 `/org/` 路由
- 需要改用户端 → 看 `/u/` 路由，参考 14-frontend-design.md
