---
phase: 01-token-core-unified
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modelcraft-backend/internal/domain/auth/issuer.go
  - modelcraft-backend/internal/domain/auth/platform_claims.go
  - modelcraft-backend/internal/domain/auth/user_claims.go
  - modelcraft-backend/internal/domain/auth/jwt_signer.go
  - modelcraft-backend/internal/domain/auth/modelcraft_claims.go
autonomous: true
requirements:
  - TOKEN-01

must_haves:
  truths:
    - "issuer.go 中存在 IssuerPlatform = \"mc-platform\" 常量，旧常量 IssuerDeveloper / IssuerEndUser 被废弃（保留 Deprecated 标注，IsValid() 只接受 mc-platform）"
    - "新文件 platform_claims.go 定义了 PlatformClaims 结构体，包含 UserID、OrgName、Scope 字段，以及 TokenScopeOrg / TokenScopeProject / TokenScopeServiceKey 常量"
    - "jwt_signer.go 中 IssueAccessToken 签名变为 (userID, orgName, scope string)，内部签发 PlatformClaims（ES256），issuer 固定为 IssuerPlatform"
    - "user_claims.go 的 Validate() 改为检查 IssuerPlatform；modelcraft_claims.go 的 Validate() 同步更新并标注 Deprecated"
    - "go test ./internal/domain/auth/... 通过，无编译错误"
  artifacts:
    - path: "modelcraft-backend/internal/domain/auth/platform_claims.go"
      provides: "统一 JWT claims 结构体，scope 常量定义"
      exports: ["PlatformClaims", "TokenScopeOrg", "TokenScopeProject", "TokenScopeServiceKey"]
    - path: "modelcraft-backend/internal/domain/auth/issuer.go"
      provides: "统一 Issuer 常量"
      contains: "IssuerPlatform"
    - path: "modelcraft-backend/internal/domain/auth/jwt_signer.go"
      provides: "ES256 Token 签发，新签名 IssueAccessToken(userID, orgName, scope string)"
  key_links:
    - from: "jwt_signer.go IssueAccessToken"
      to: "platform_claims.go PlatformClaims"
      via: "claims := &PlatformClaims{...}"
      pattern: "PlatformClaims"
    - from: "user_claims.go Validate()"
      to: "issuer.go IssuerPlatform"
      via: "c.Issuer != string(IssuerPlatform)"
      pattern: "IssuerPlatform"
---

<objective>
统一 JWT 领域层核心定义：废弃 mc-developer / mc-enduser 双 issuer，引入单一 mc-platform issuer 和 PlatformClaims 结构体，为后续应用层和接口层改造奠定基础。

**目的**：消除领域层的 issuer 分叉，所有 JWT 签发和验证从此依赖同一套常量和 claims 结构。  
**输出**：4 个修改文件 + 1 个新文件，Go 编译通过。
</objective>

<execution_context>
@/data/home/lukemxjia/modelcraft/.codebuddy/get-shit-done/workflows/execute-plan.md
@/data/home/lukemxjia/modelcraft/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-token-core-unified/01-RESEARCH.md
@modelcraft-backend/internal/domain/auth/issuer.go
@modelcraft-backend/internal/domain/auth/user_claims.go
@modelcraft-backend/internal/domain/auth/jwt_signer.go
@modelcraft-backend/internal/domain/auth/modelcraft_claims.go

<interfaces>
<!-- 执行者直接使用，无需再读原始文件 -->

当前 issuer.go（需全部替换）：
```go
const (
    IssuerDeveloper Issuer = "mc-developer"
    IssuerEndUser   Issuer = "mc-enduser"
)
func (i Issuer) IsValid() bool {
    switch i { case IssuerDeveloper, IssuerEndUser: return true }
    return false
}
```

当前 jwt_signer.go 中 IssueAccessToken 签名（需变更）：
```go
func (s *JWTSigner) IssueAccessToken(userID, userName string) (string, error) {
    claims := &UserClaims{ UserID: userID, ... }
    _ = userName
    ...
}
```

当前 user_claims.go Validate() 关键行（需变更）：
```go
if c.Issuer != string(IssuerDeveloper) {  // 第 33 行
    return fmt.Errorf("%w: expected issuer '%s'...", ...)
}
```

当前 modelcraft_claims.go Validate() 关键行（需变更）：
```go
if c.Issuer != string(IssuerDeveloper) {  // 第 60 行
```

GenerateDevSigner() 中硬编码 issuer（需变更）：
```go
issuer: string(IssuerDeveloper),  // 第 84 行
```

NewJWTSignerFromPEM 中 fallback（需变更）：
```go
issuer = string(IssuerDeveloper)  // 第 69 行
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：新建 platform_claims.go，定义统一 PlatformClaims 和常量</name>
  <files>
    modelcraft-backend/internal/domain/auth/platform_claims.go
    modelcraft-backend/internal/domain/auth/platform_claims_test.go
  </files>
  <behavior>
    - PlatformClaims.Validate() 成功：UserID 非空、OrgName 非空、Scope 为 org/project/service_key 之一、Issuer == "mc-platform"、未过期 → 返回 nil
    - PlatformClaims.Validate() 失败：UserID 为空 → 返回包含 "user_id is required" 的错误
    - PlatformClaims.Validate() 失败：OrgName 为空 → 返回包含 "org_name is required" 的错误
    - PlatformClaims.Validate() 失败：Scope 为 "" 或非法值 → 返回包含 "scope" 的错误
    - PlatformClaims.Validate() 失败：Issuer != "mc-platform" → 返回包含 "expected issuer" 的错误
    - PlatformClaims.Validate() 失败：已过期 → 返回 ErrTokenExpired
    - TokenScopeOrg == "org"、TokenScopeProject == "project"、TokenScopeServiceKey == "service_key"
  </behavior>
  <action>
创建 `modelcraft-backend/internal/domain/auth/platform_claims.go`，内容如下：

```go
package auth

import (
    "errors"
    "fmt"
    "time"

    "github.com/golang-jwt/jwt/v5"
)

// PlatformClaims 是统一 JWT 的 payload 结构，适用于所有用户类型（平台管理员 / 端用户）。
// 通过 Scope 字段区分 token 的访问范围，废弃了原 mc-developer / mc-enduser 双 issuer 设计。
type PlatformClaims struct {
    UserID  string `json:"user_id"`
    OrgName string `json:"org_name"`
    // Scope 标识 token 的访问范围："org" | "project" | "service_key"（预留）
    Scope string `json:"scope"`
    jwt.RegisteredClaims
}

const (
    // TokenScopeOrg 表示 Org 级别 token，可访问 org 管理接口
    TokenScopeOrg = "org"
    // TokenScopeProject 表示 Project 级别 token，可访问 project 数据接口
    TokenScopeProject = "project"
    // TokenScopeServiceKey 预留，本期不签发
    TokenScopeServiceKey = "service_key"
)

var ErrPlatformClaimsInvalid = errors.New("invalid platform claims")

// Validate 校验 PlatformClaims 的完整性和有效性。
func (c *PlatformClaims) Validate() error {
    if c.UserID == "" {
        return fmt.Errorf("%w: user_id is required", ErrPlatformClaimsInvalid)
    }
    if c.OrgName == "" {
        return fmt.Errorf("%w: org_name is required", ErrPlatformClaimsInvalid)
    }
    if c.Scope != TokenScopeOrg && c.Scope != TokenScopeProject && c.Scope != TokenScopeServiceKey {
        return fmt.Errorf("%w: invalid scope %q, must be one of org|project|service_key", ErrPlatformClaimsInvalid, c.Scope)
    }
    if c.Issuer != string(IssuerPlatform) {
        return fmt.Errorf("%w: expected issuer %q, got %q", ErrPlatformClaimsInvalid, IssuerPlatform, c.Issuer)
    }
    if c.ExpiresAt == nil || c.ExpiresAt.Before(time.Now()) {
        return ErrTokenExpired
    }
    return nil
}

// IsExpired 检查 token 是否已过期。
func (c *PlatformClaims) IsExpired() bool {
    if c.ExpiresAt == nil {
        return true
    }
    return !time.Now().Before(c.ExpiresAt.Time)
}
```

然后创建 `platform_claims_test.go`，编写覆盖上述所有行为的单元测试（TDD 流程：先跑测试确认红，再写实现，再跑测试确认绿）。

**注意**：`IssuerPlatform` 在任务 2 中定义，先在此文件中引用，任务 2 完成后再统一编译验证。若需要独立验证，可在 platform_claims.go 顶部暂时声明 `const IssuerPlatform Issuer = "mc-platform"` 并在任务 2 完成后删除。推荐方案：先完成任务 2 再写测试。
  </action>
  <verify>
    <automated>cd /data/home/lukemxjia/modelcraft/modelcraft-backend && go test ./internal/domain/auth/... -run TestPlatformClaims -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - platform_claims.go 文件存在，包含 PlatformClaims 结构体和 TokenScopeOrg/Project/ServiceKey 常量
    - platform_claims_test.go 存在，TestPlatformClaims 所有子测试 PASS
    - grep -c "TokenScopeOrg" platform_claims.go 输出 >= 1
  </done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：更新 issuer.go，迁移旧常量；更新 jwt_signer.go 签名</name>
  <files>
    modelcraft-backend/internal/domain/auth/issuer.go
    modelcraft-backend/internal/domain/auth/jwt_signer.go
    modelcraft-backend/internal/domain/auth/user_claims.go
    modelcraft-backend/internal/domain/auth/modelcraft_claims.go
    modelcraft-backend/internal/domain/auth/jwt_signer_test.go
  </files>
  <behavior>
    - issuer.go：IssuerPlatform = "mc-platform" 存在；IsValid() 对 "mc-platform" 返回 true，对 "mc-developer"/"mc-enduser" 返回 false
    - jwt_signer.go：IssueAccessToken(userID, orgName, scope string) 签发 PlatformClaims，issuer 固定为 IssuerPlatform（ES256）
    - jwt_signer.go：IssueAccessToken 入参 orgName 为空 → 返回非空 error
    - jwt_signer.go：IssueAccessToken 入参 scope 非法 → 返回非空 error
    - jwt_signer.go：GenerateDevSigner() 中默认 issuer 改为 IssuerPlatform
    - user_claims.go：Validate() 检查 c.Issuer != string(IssuerPlatform) → 返回错误
    - modelcraft_claims.go：Validate() 检查 IssuerPlatform，顶部添加 // Deprecated 注释
  </behavior>
  <action>
**第一步：修改 issuer.go**

将文件内容替换为：
```go
package auth

// Issuer JWT 签发者
type Issuer string

const (
    // IssuerPlatform 是统一的 JWT 签发者标识，所有 token 均使用此值。
    IssuerPlatform Issuer = "mc-platform"

    // Deprecated: IssuerDeveloper 已废弃，由 IssuerPlatform 替代。硬切后不再有效。
    IssuerDeveloper Issuer = "mc-developer"

    // Deprecated: IssuerEndUser 已废弃，由 IssuerPlatform 替代。硬切后不再有效。
    IssuerEndUser Issuer = "mc-enduser"
)

// IsValid 判断是否为合法的 Issuer。只有 IssuerPlatform 有效。
func (i Issuer) IsValid() bool {
    return i == IssuerPlatform
}
```

**第二步：修改 jwt_signer.go**

1. 将 `IssueAccessToken(userID, userName string)` 签名改为 `IssueAccessToken(userID, orgName, scope string)`
2. 函数体内：验证 orgName 非空（否则返回 `errors.New("jwt_signer: orgName is required")`），验证 scope 在 TokenScopeOrg/Project/ServiceKey 中（否则 `fmt.Errorf("jwt_signer: invalid scope %q", scope)`）
3. 将 claims 从 `&UserClaims{...}` 改为 `&PlatformClaims{UserID: userID, OrgName: orgName, Scope: scope, RegisteredClaims: jwt.RegisteredClaims{Issuer: string(IssuerPlatform), ...}}`
4. `NewJWTSignerFromPEM` 中 fallback：`issuer = string(IssuerPlatform)`（原为 IssuerDeveloper）
5. `GenerateDevSigner()` 中：`issuer: string(IssuerPlatform)`（原为 IssuerDeveloper）

**第三步：修改 user_claims.go**

将第 33 行 `IssuerDeveloper` 改为 `IssuerPlatform`：
```go
if c.Issuer != string(IssuerPlatform) {
    return fmt.Errorf("%w: expected issuer '%s', got '%s'", ErrUserClaimsInvalid, IssuerPlatform, c.Issuer)
}
```

**第四步：修改 modelcraft_claims.go**

1. 在文件头部（package 声明下方）添加注释：
```go
// Deprecated: ModelCraftClaims 不再用于签发 Token，由 PlatformClaims 替代。
// 保留此文件仅为向后兼容，未来阶段将删除。
```
2. 将 Validate() 中第 60 行 `IssuerDeveloper` 改为 `IssuerPlatform`：
```go
if c.Issuer != string(IssuerPlatform) {
    return fmt.Errorf("%w: expected issuer '%s', got '%s'", ErrInvalidToken, IssuerPlatform, c.Issuer)
}
```

**第五步：编写 jwt_signer_test.go**（TDD：先红后绿）

覆盖 IssueAccessToken 新签名的行为测试：
- 合法入参（userID + orgName + scope=org）→ 返回有效 JWT，解析后 Claims.OrgName 正确，Claims.Scope=="org"，Claims.Issuer=="mc-platform"
- orgName 为空 → 返回 error
- scope 非法 → 返回 error
  </action>
  <verify>
    <automated>cd /data/home/lukemxjia/modelcraft/modelcraft-backend && go test ./internal/domain/auth/... -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "IssuerPlatform" modelcraft-backend/internal/domain/auth/issuer.go` 输出 >= 1
    - `grep "mc-platform" modelcraft-backend/internal/domain/auth/issuer.go` 输出 `IssuerPlatform Issuer = "mc-platform"`
    - `grep "IssuerPlatform" modelcraft-backend/internal/domain/auth/jwt_signer.go` 输出至少 2 行（fallback + PlatformClaims 初始化）
    - `grep "IssuerPlatform" modelcraft-backend/internal/domain/auth/user_claims.go` 输出 1 行
    - `grep "Deprecated" modelcraft-backend/internal/domain/auth/modelcraft_claims.go` 输出 1 行
    - `cd modelcraft-backend && go test ./internal/domain/auth/... 2>&1 | grep -c "FAIL"` 输出 0
  </acceptance_criteria>
  <done>
    - issuer.go 只有 IssuerPlatform 通过 IsValid() 验证
    - jwt_signer.go IssueAccessToken 新签名签发 PlatformClaims
    - user_claims.go 和 modelcraft_claims.go 均检查 IssuerPlatform
    - go test ./internal/domain/auth/... 全部通过
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| JWT payload → Validate() | claims 来自客户端，需严格验证 issuer/scope/expiry |
| GenerateDevSigner() | 开发模式下私钥不持久化，测试 token 不应流入生产 |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 处置 | 缓解措施 |
|---------|------|------|------|----------|
| T-01-01 | 篡改（T） | PlatformClaims.Scope | 缓解 | Validate() 严格校验 scope 只能是 org/project/service_key |
| T-01-02 | 欺骗（S） | issuer 字符串 | 缓解 | IsValid() 只接受 mc-platform；旧 issuer 立即无效 |
| T-01-03 | 信息泄露（I） | GenerateDevSigner 私钥 | 接受 | 开发模式设计；生产必须配置 PEM 私钥（cfg.JWT.PrivateKey 非空） |
| T-01-04 | 否认（R） | 旧 IssuerDeveloper/IssuerEndUser 常量保留 | 接受 | 保留仅为 IDE/grep 可追踪；IsValid() 不接受，运行时无影响 |
</threat_model>

<verification>
```bash
# 整体编译验证
cd /data/home/lukemxjia/modelcraft/modelcraft-backend && go build ./...

# 领域层单元测试
go test ./internal/domain/auth/... -v

# Lint（pre-commit hook 要求）
just lint
```
</verification>

<success_criteria>
1. `go build ./internal/domain/auth/...` 无错误
2. `go test ./internal/domain/auth/...` 全部 PASS，无 FAIL
3. `grep -r "IssuerDeveloper\|IssuerEndUser" internal/domain/auth/*.go | grep -v "Deprecated\|mc-developer\|mc-enduser"` 输出为空（只剩 Deprecated 注释行）
4. `just lint` 通过
</success_criteria>

<output>
完成后，创建 `.planning/phases/01-token-core-unified/01-01-SUMMARY.md`，记录：
- 完成的文件列表及关键变更
- platform_claims.go 的 PlatformClaims 结构体字段
- IssueAccessToken 新签名（供后续 Wave 2 计划引用）
- 遇到的问题和解决方案
</output>
