---
phase: "03"
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-PLAN-01"
files_modified:
  - modelcraft-backend/internal/interfaces/graphql/enduser/  # 整个目录删除
  - modelcraft-backend/api/graph/end_user/                   # 整个目录删除
  - modelcraft-backend/gqlgen.end_user.yml                   # 文件删除
  - modelcraft-backend/justfile                              # 删除 generate-gql 中的 end_user 行
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02

must_haves:
  truths:
    - "internal/interfaces/graphql/enduser/ 目录在代码库中不存在"
    - "api/graph/end_user/ 目录在代码库中不存在"
    - "gqlgen.end_user.yml 文件不存在"
    - "justfile 的 generate-gql recipe 不再调用 gqlgen.end_user.yml"
    - "just generate-gql 通过（org + project 两套 schema 正常生成，无报错）"
    - "just build 通过，无编译错误"
    - "just lint 通过，无 lint 错误"
    - "原 end_user schema 9 条 query 均已处置：4 条直接删除（modelDatabaseCatalog/modelCatalog/model/projects），2 条有 project schema 替代（user/users），3 条 meta/user 暂时删除待阶段 4（me/findOne/findMany）；处置记录见 RESEARCH.md §3"
  artifacts:
    - path: "modelcraft-backend/justfile"
      provides: "generate-gql recipe（已移除 end_user 配置行）"
  key_links:
    - from: "justfile:generate-gql"
      to: "gqlgen.end_user.yml"
      via: "删除该 recipe 行"
      pattern: "gqlgen.end_user.yml"
---

<objective>
删除 end_user GraphQL 的全部核心组成：handler/resolver 目录、schema 文件目录、gqlgen 配置，并同步更新 justfile 中的代码生成脚本。

Purpose: Wave 1 已切断路由层引用，现在可以安全删除整个 enduser 包目录。删除后通过 `just build` 和 `just lint` 验证无残留依赖。
Output: enduser 包及相关 schema 文件、gqlgen 配置全部从代码库删除，justfile 中不再生成 end_user 代码。
</objective>

<execution_context>
@/data/home/lukemxjia/modelcraft/.codebuddy/get-shit-done/workflows/execute-plan.md
@/data/home/lukemxjia/modelcraft/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-end-user-schema-cleanup/03-RESEARCH.md
@.planning/phases/03-end-user-schema-cleanup/03-01-SUMMARY.md

<!-- 删除目标清单（共 20 个文件 + 3 个目录）：

  目录级删除（rm -rf）：
    modelcraft-backend/internal/interfaces/graphql/enduser/          # 含 generated/ 子目录，共 14 个文件
    modelcraft-backend/api/graph/end_user/schema/                    # 6 个 .graphql 文件

  单文件删除：
    modelcraft-backend/gqlgen.end_user.yml

  justfile 修改（删除一行）：
    第 300 行：go run github.com/99designs/gqlgen generate --config gqlgen.end_user.yml

  重要：禁止运行 just clean-gql（会删除已实现的 org/project resolver）
  重要：删除 gqlgen.end_user.yml 后不需要运行 just generate-gql（end_user schema 已彻底删除）
-->

<!-- 保留确认（不删除这些）：
  - internal/interfaces/graphql/org/end_user.resolvers.go    ← org schema 的 enduser 管理 resolver，与 end_user schema 无关
  - internal/interfaces/graphql/project/end_user.resolvers.go ← project schema 的 enduser 管理 resolver，保留
  - internal/app/enduser/                                     ← app 层业务服务，被 HTTP handler 引用，保留
  - internal/interfaces/http/handlers/enduser/               ← REST handler，保留
-->
</context>

<tasks>

<task type="auto">
  <name>任务 1：删除 enduser 包目录（resolver + 生成代码）和 Schema 文件目录</name>
  <read_first>
    - modelcraft-backend/internal/interfaces/graphql/enduser/（确认目录内容）
    - modelcraft-backend/api/graph/end_user/（确认目录内容）
    - modelcraft-backend/gqlgen.end_user.yml（确认文件存在）
  </read_first>
  <files>
    modelcraft-backend/internal/interfaces/graphql/enduser/（整个目录删除）
    modelcraft-backend/api/graph/end_user/（整个目录删除）
    modelcraft-backend/gqlgen.end_user.yml
  </files>
  <action>
    执行以下三条删除命令（在 modelcraft-backend/ 目录下执行）：

    ```bash
    # 1. 删除 enduser resolver 目录（含 generated/ 子目录，共 14 个文件）
    rm -rf internal/interfaces/graphql/enduser/

    # 2. 删除 end_user schema 文件目录（6 个 .graphql 文件）
    rm -rf api/graph/end_user/

    # 3. 删除 gqlgen 配置文件
    rm gqlgen.end_user.yml
    ```

    删除前验证：
    - `ls internal/interfaces/graphql/enduser/` 应列出 resolver 文件
    - `ls api/graph/end_user/schema/` 应列出 .graphql 文件
    - 确认 `internal/interfaces/graphql/org/end_user.resolvers.go` 仍存在（保留）
    - 确认 `internal/interfaces/graphql/project/end_user.resolvers.go` 仍存在（保留）
  </action>
  <verify>
    <automated>cd modelcraft-backend && test ! -d internal/interfaces/graphql/enduser && test ! -d api/graph/end_user && test ! -f gqlgen.end_user.yml && go build ./... && echo "DELETED AND COMPILED OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d modelcraft-backend/internal/interfaces/graphql/enduser` 返回 0（目录不存在）
    - `test ! -d modelcraft-backend/api/graph/end_user` 返回 0
    - `test ! -f modelcraft-backend/gqlgen.end_user.yml` 返回 0
    - `test -f modelcraft-backend/internal/interfaces/graphql/org/end_user.resolvers.go` 返回 0（保留文件存在）
    - `test -f modelcraft-backend/internal/interfaces/graphql/project/end_user.resolvers.go` 返回 0（保留文件存在）
  </acceptance_criteria>
  <done>20 个 end_user 相关文件及目录已删除，org/project schema 的 end_user resolver 完好保留</done>
</task>

<task type="auto">
  <name>任务 2：更新 justfile 并验证全量编译通过</name>
  <read_first>
    - modelcraft-backend/justfile（第 293-302 行，generate-gql recipe 区域）
  </read_first>
  <files>modelcraft-backend/justfile</files>
  <action>
    **修改 justfile**（第 300 行）：
    在 `generate-gql` recipe 中，找到并删除以下一行：
    ```
        go run github.com/99designs/gqlgen generate --config gqlgen.end_user.yml
    ```
    保留其他两行（gqlgen.org.yml 和 gqlgen.project.yml），结果应如下：
    ```just
    # 生成 GraphQL 代码
    [doc("Generate GraphQL code")]
    generate-gql: install-gqlgen
        echo "📊 生成 GraphQL 代码..."
        go run github.com/99designs/gqlgen generate --config gqlgen.org.yml
        go run github.com/99designs/gqlgen generate --config gqlgen.project.yml
        echo "✅ GraphQL 代码生成完成"
    ```

    **修改后运行编译验证**：
    ```bash
    cd modelcraft-backend && just build
    ```

    如果 `just build` 失败，检查错误信息：
    - 若提示 undefined: `appEnduser` 之类的错误，说明 routes.go 中 appEnduser import 未在 Wave 1 正确处理，补充删除
    - 若提示其他未解决的依赖，根据错误信息定位并修复（不涉及 org/project schema 的文件）

    **运行 lint 验证**：
    ```bash
    cd modelcraft-backend && just lint
    ```
  </action>
  <verify>
    <automated>cd modelcraft-backend && just build && just lint && just generate-gql && echo "ALL WAVE2 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gqlgen.end_user.yml' modelcraft-backend/justfile` 输出 `0`
    - `cd modelcraft-backend && just build` 执行成功，退出码为 0，无编译错误
    - `cd modelcraft-backend && just lint` 执行成功，退出码为 0
    - `cd modelcraft-backend && just generate-gql` 执行成功，退出码为 0（org + project 两套 schema 正常生成）
    - `grep -rn 'endusergraphql\|graphql/enduser' modelcraft-backend/internal/ --include='*.go' | grep -v '_test.go'` 输出为空（无残留引用）
  </acceptance_criteria>
  <done>justfile 已更新，后端全量编译和 lint 通过，无 end_user GraphQL 包残留引用</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 删除操作 → 编译验证 | 文件删除是不可逆的，必须通过 just build 确认无残留依赖 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | 误删 org/project end_user resolver | mitigate | 删除前检查路径精确性，删除后用 `test -f` 确认保留文件存在 |
| T-03-04 | Denial of Service | just build 失败 | mitigate | 失败后分析错误信息，精准修复残留引用，不扩大删除范围 |
| T-03-05 | Tampering | 运行 just clean-gql | mitigate | 明确禁止，只运行 just build 和 just lint |
</threat_model>

<verification>
Wave 2 整体验证：
```bash
# 确认目录已删除
test ! -d modelcraft-backend/internal/interfaces/graphql/enduser && echo "enduser dir: DELETED"
test ! -d modelcraft-backend/api/graph/end_user && echo "schema dir: DELETED"
test ! -f modelcraft-backend/gqlgen.end_user.yml && echo "gqlgen config: DELETED"

# 确认保留文件完好
test -f modelcraft-backend/internal/interfaces/graphql/org/end_user.resolvers.go && echo "org resolver: OK"
test -f modelcraft-backend/internal/interfaces/graphql/project/end_user.resolvers.go && echo "project resolver: OK"

# 确认编译和 lint 通过
cd modelcraft-backend && just build && just lint

# 确认无残留引用
grep -rn 'endusergraphql' modelcraft-backend/internal/ --include='*.go' | wc -l
# 预期输出：0
```
</verification>

<success_criteria>
1. `internal/interfaces/graphql/enduser/` 目录不存在
2. `api/graph/end_user/` 目录不存在
3. `gqlgen.end_user.yml` 文件不存在
4. `justfile` 的 `generate-gql` recipe 中无 `gqlgen.end_user.yml` 引用
5. `just build` 通过（退出码 0）
6. `just lint` 通过（退出码 0）
7. org/project schema 的 end_user resolver 文件完好
</success_criteria>

<output>
完成后创建 `.planning/phases/03-end-user-schema-cleanup/03-02-SUMMARY.md`
</output>
