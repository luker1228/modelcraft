---
phase: "03"
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-PLAN-02"
files_modified:
  - modelcraft-gateway/cmd/gateway/main.go
  - modelcraft-gateway/internal/proxy/handler.go
  - modelcraft-gateway/internal/auth/service.go
  - modelcraft-gateway/internal/config/config.go
autonomous: true
requirements:
  - SCHEMA-01

must_haves:
  truths:
    - "gateway 不再注册 /graphql/end-user/* 路由"
    - "EndUserGraphQLHandler 函数已从 proxy/handler.go 删除"
    - "EndUserClaims 结构体和 VerifyEndUserAccessToken 方法已从 auth/service.go 删除"
    - "EndUserJWTSecret 字段已从 config/config.go 删除"
    - "auth.NewService 签名已移除 endUserSecret 参数"
    - "cd modelcraft-gateway && go build ./... 通过"
  artifacts:
    - path: "modelcraft-gateway/cmd/gateway/main.go"
      provides: "Gateway 路由注册（不含 end-user GraphQL 路由）"
    - path: "modelcraft-gateway/internal/proxy/handler.go"
      provides: "Gateway proxy handler（EndUserGraphQLHandler 已删除）"
    - path: "modelcraft-gateway/internal/auth/service.go"
      provides: "Gateway auth service（Deprecated HMAC 代码已清理）"
    - path: "modelcraft-gateway/internal/config/config.go"
      provides: "Gateway config（EndUserJWTSecret 字段已删除）"
  key_links:
    - from: "main.go"
      to: "auth.NewService"
      via: "签名更新（移除 endUserSecret 参数）"
      pattern: "auth.NewService"
    - from: "main.go"
      to: "proxyHandler.EndUserGraphQLHandler"
      via: "删除路由注册"
      pattern: "EndUserGraphQLHandler"
---

<objective>
清理 Gateway 侧的 end_user GraphQL 路由注册、proxy handler，以及 auth service 和 config 中标注为 Deprecated 的 HMAC 遗留代码。

Purpose: Backend 的 end_user schema 已在 Wave 2 删除，Gateway 侧还保留着指向该 schema 的路由代理和废弃的 HMAC 验证代码，需要一并清除。Gateway 是独立 Go module，需单独验证编译。
Output: Gateway 不再代理 /graphql/end-user/* 路由；Deprecated 的 EndUserClaims / VerifyEndUserAccessToken / EndUserJWTSecret 全部从代码中移除。
</objective>

<execution_context>
@/data/home/lukemxjia/modelcraft/.codebuddy/get-shit-done/workflows/execute-plan.md
@/data/home/lukemxjia/modelcraft/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-end-user-schema-cleanup/03-RESEARCH.md
@.planning/phases/03-end-user-schema-cleanup/03-02-SUMMARY.md

<!-- 删改细节：

  main.go（modelcraft-gateway/cmd/gateway/main.go）：
    - 删除第 111-113 行（含注释块）：
        // End-User GraphQL — end-user HMAC JWT required.
        r.Post("/graphql/end-user/org/{orgName}/project/{projectSlug}", proxyHandler.EndUserGraphQLHandler)
        r.Post("/graphql/end-user/org/{orgName}/project/{projectSlug}/", proxyHandler.EndUserGraphQLHandler)
    - 删除第 49 行 cfg.EndUserJWTSecret 参数传入，并更新 auth.NewService 调用（移除最后一个参数）

  proxy/handler.go（modelcraft-gateway/internal/proxy/handler.go）：
    - 删除 EndUserGraphQLHandler 函数（第 113-146 行，含注释）

  auth/service.go（modelcraft-gateway/internal/auth/service.go）：
    - 删除 endUserJWTSecret []byte 字段（第 28 行）
    - 删除 NewService 函数中 endUserSecret string 参数和对应的 endUserJWTSecret: []byte(endUserSecret) 赋值（第 35 行签名，第 53 行赋值）
    - 删除 EndUserClaims 结构体（第 140-146 行）
    - 删除 VerifyEndUserAccessToken 方法（第 148-182 行）

  config/config.go（modelcraft-gateway/internal/config/config.go）：
    - 删除 EndUserJWTSecret string 字段（第 23 行，含 Deprecated 注释）
    - 删除对应 getEnv("JWT_SECRET", "") 赋值（第 51 行）

  保留（不删除）：
    - endUserRefreshCookieName 字段（第 31 行）—— 端用户 refresh cookie 管理仍需要
    - SetEndUserRefreshCookie / GetEndUserRefreshCookie / ClearEndUserRefreshCookie 方法 —— REST 认证路由仍使用
    - auth/handler.go 中 EndUserLogin/Refresh/Logout/Me 代理方法 —— 端用户 REST 认证保留
-->
</context>

<tasks>

<task type="auto">
  <name>任务 1：清理 main.go 路由注册和 auth.NewService 调用</name>
  <read_first>
    - modelcraft-gateway/cmd/gateway/main.go（全文，重点第 40-55 行 auth.NewService 调用区，第 105-120 行路由注册区）
  </read_first>
  <files>modelcraft-gateway/cmd/gateway/main.go</files>
  <action>
    在 main.go 中执行两处修改：

    **修改 1 — 删除 End-User GraphQL 路由注册（第 111-113 行附近）**：
    删除以下代码块（含注释）：
    ```go
    // End-User GraphQL — end-user HMAC JWT required.
    r.Post("/graphql/end-user/org/{orgName}/project/{projectSlug}", proxyHandler.EndUserGraphQLHandler)
    r.Post("/graphql/end-user/org/{orgName}/project/{projectSlug}/", proxyHandler.EndUserGraphQLHandler)
    ```

    **修改 2 — 更新 auth.NewService 调用（第 44-50 行附近）**：
    原调用：
    ```go
    authSvc, err := auth.NewService(
        cfg.JWTPublicKey,
        cfg.RefreshTokenTTL,
        cfg.RefreshCookieName,
        cfg.EndUserRefreshCookieName,
        cfg.EndUserJWTSecret, // Deprecated: 端用户 token 已迁移 ES256，此参数将在阶段 3 移除
    )
    ```
    修改为（移除最后一个参数）：
    ```go
    authSvc, err := auth.NewService(
        cfg.JWTPublicKey,
        cfg.RefreshTokenTTL,
        cfg.RefreshCookieName,
        cfg.EndUserRefreshCookieName,
    )
    ```
    同时删除 `cfg.EndUserJWTSecret` 那行（含注释）。

    注意：auth.NewService 函数签名将在任务 2 中更新，main.go 和 service.go 必须同一 Wave 修改，否则编译失败。
  </action>
  <verify>
    <automated>grep -c 'EndUserJWTSecret\|EndUserGraphQLHandler' modelcraft-gateway/cmd/gateway/main.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'EndUserGraphQLHandler' modelcraft-gateway/cmd/gateway/main.go` 输出 `0`
    - `grep -c 'EndUserJWTSecret' modelcraft-gateway/cmd/gateway/main.go` 输出 `0`
    - `grep -c 'graphql/end-user' modelcraft-gateway/cmd/gateway/main.go` 输出 `0`
  </acceptance_criteria>
  <done>main.go 中 end-user GraphQL 路由注册已删除，auth.NewService 调用已更新</done>
</task>

<task type="auto">
  <name>任务 2：删除 EndUserGraphQLHandler、Deprecated auth 代码，并验证 gateway 编译</name>
  <read_first>
    - modelcraft-gateway/internal/proxy/handler.go（第 110-150 行，EndUserGraphQLHandler 区域）
    - modelcraft-gateway/internal/auth/service.go（全文，重点第 20-60 行 struct/NewService，第 138-185 行 Deprecated 区域）
    - modelcraft-gateway/internal/config/config.go（全文，重点第 15-55 行 struct 和 Load 函数）
  </read_first>
  <files>
    modelcraft-gateway/internal/proxy/handler.go
    modelcraft-gateway/internal/auth/service.go
    modelcraft-gateway/internal/config/config.go
  </files>
  <action>
    **proxy/handler.go — 删除 EndUserGraphQLHandler 函数**：
    删除从注释 `// EndUserGraphQLHandler 验证端用户 Bearer JWT...` 到函数结尾 `}` 的完整代码块（约 35 行）。
    注意：保留后面的 `extractAndVerify` 私有方法及其他 handler 方法不变。

    **auth/service.go — 三处修改**：

    1. 删除 `Service` struct 中的 `endUserJWTSecret []byte` 字段（含 Deprecated 注释，约 2 行）。

    2. 更新 `NewService` 函数签名：
       - 删除参数 `endUserSecret string`（最后一个参数）
       - 删除函数体内 `endUserJWTSecret: []byte(endUserSecret),` 赋值行
       
       原签名：`func NewService(publicKeyPEM string, refreshTTL time.Duration, cookieName string, endUserCookieName string, endUserSecret string) (*Service, error)`
       新签名：`func NewService(publicKeyPEM string, refreshTTL time.Duration, cookieName string, endUserCookieName string) (*Service, error)`

    3. 删除 Deprecated 的 `EndUserClaims` 结构体（含注释，约 7 行）和
       `VerifyEndUserAccessToken` 方法（含注释，约 35 行）。
       保留：`GetEndUserRefreshCookie`、`SetEndUserRefreshCookie`（如有）、`ClearEndUserRefreshCookie`（如有）。

    **config/config.go — 两处修改**：

    1. 删除 `Config` struct 中的 `EndUserJWTSecret string` 字段（含 Deprecated 注释，约 2 行）。
    
    2. 删除 `Load()` 或配置初始化函数中对应的赋值：
       `EndUserJWTSecret: getEnv("JWT_SECRET", ""),`

    **验证 gateway 编译**：
    ```bash
    cd modelcraft-gateway && go build ./...
    ```
  </action>
  <verify>
    <automated>cd modelcraft-gateway && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'EndUserGraphQLHandler' modelcraft-gateway/internal/proxy/handler.go` 输出 `0`
    - `grep -c 'EndUserClaims\|VerifyEndUserAccessToken\|endUserJWTSecret' modelcraft-gateway/internal/auth/service.go` 输出 `0`
    - `grep -c 'EndUserJWTSecret' modelcraft-gateway/internal/config/config.go` 输出 `0`
    - `cd modelcraft-gateway && go build ./...` 执行成功，退出码为 0
    - `grep -c 'endUserRefreshCookieName' modelcraft-gateway/internal/auth/service.go` 输出大于 `0`（保留字段）
  </acceptance_criteria>
  <done>gateway 的 Deprecated 代码已清理，go build ./... 通过，端用户 refresh cookie 相关代码保留完好</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main.go 签名变更 → service.go 签名变更 | 两者必须同一 Wave 修改，否则 go build 报参数数量不匹配 |
| gateway 独立模块 → backend 编译 | gateway 是独立 Go module，需单独 cd 到 modelcraft-gateway/ 执行 go build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | 误删 endUserRefreshCookieName 字段 | mitigate | 删除后用 grep 确认 endUserRefreshCookieName 字段仍在 service.go |
| T-03-07 | Denial of Service | NewService 签名参数数量不匹配 | mitigate | main.go 和 service.go 在同一任务内同步修改，修改后立即 go build 验证 |
| T-03-08 | Repudiation | gateway 路由残留 | accept | go build 通过后，路由注册代码不存在，访问 /graphql/end-user/* 自然返回 404 |
</threat_model>

<verification>
Wave 3 整体验证：
```bash
# 确认路由已删除
grep -c 'graphql/end-user' modelcraft-gateway/cmd/gateway/main.go
# 预期：0

# 确认 Deprecated 代码已清理
grep -c 'EndUserClaims\|VerifyEndUserAccessToken\|endUserJWTSecret\|EndUserJWTSecret' \
  modelcraft-gateway/internal/auth/service.go \
  modelcraft-gateway/internal/config/config.go \
  modelcraft-gateway/internal/proxy/handler.go
# 预期：0

# 确认保留代码完好
grep -c 'endUserRefreshCookieName' modelcraft-gateway/internal/auth/service.go
# 预期：>0

# 编译验证（gateway 独立 module）
cd modelcraft-gateway && go build ./...
# 预期：无错误输出，退出码 0
```
</verification>

<success_criteria>
1. `main.go` 中不含 `/graphql/end-user/` 路由注册，`EndUserJWTSecret` 参数已移除
2. `proxy/handler.go` 中 `EndUserGraphQLHandler` 函数已删除
3. `auth/service.go` 中 `EndUserClaims`、`VerifyEndUserAccessToken`、`endUserJWTSecret` 字段已删除，`NewService` 签名更新
4. `config/config.go` 中 `EndUserJWTSecret` 字段已删除
5. `endUserRefreshCookieName` 及 cookie 管理方法完好保留
6. `cd modelcraft-gateway && go build ./...` 通过（退出码 0）
7. 阶段 3 整体：访问 `/graphql/end-user/*` 在 gateway 层无路由注册（自然返回 404）
</success_criteria>

<output>
完成后创建 `.planning/phases/03-end-user-schema-cleanup/03-03-SUMMARY.md`
</output>
