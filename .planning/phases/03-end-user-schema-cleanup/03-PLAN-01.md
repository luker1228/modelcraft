---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modelcraft-backend/internal/interfaces/http/routes.go
  - modelcraft-backend/internal/interfaces/http/chi_setup.go
autonomous: true
requirements:
  - SCHEMA-01

must_haves:
  truths:
    - "routes.go 不再 import endusergraphql 包"
    - "SetupEndUserGraphQLRoutesOnChi 函数已从 routes.go 删除"
    - "chi_setup.go 不再调用 SetupEndUserGraphQLRoutesOnChi"
    - "go build ./internal/interfaces/http/... 编译通过，无悬空 import"
  artifacts:
    - path: "modelcraft-backend/internal/interfaces/http/routes.go"
      provides: "后端 chi 路由注册（已移除 end_user GraphQL 函数）"
    - path: "modelcraft-backend/internal/interfaces/http/chi_setup.go"
      provides: "chi 路由组装（已移除 end_user GraphQL 调用块）"
  key_links:
    - from: "chi_setup.go"
      to: "routes.go:SetupEndUserGraphQLRoutesOnChi"
      via: "删除调用"
      pattern: "SetupEndUserGraphQLRoutesOnChi"
    - from: "routes.go"
      to: "endusergraphql 包"
      via: "删除 import"
      pattern: "endusergraphql"
---

<objective>
切断 Backend 路由层对 endusergraphql 包的所有引用，为后续整体删除包目录扫清编译依赖。

Purpose: 删除包之前必须先删除所有调用方，否则 Wave 2 删目录后会出现悬空 import 编译错误。
Output: routes.go 中 `SetupEndUserGraphQLRoutesOnChi` 函数及其 import 被删除；chi_setup.go 中对应的调用块被删除。
</objective>

<execution_context>
@/data/home/lukemxjia/modelcraft/.codebuddy/get-shit-done/workflows/execute-plan.md
@/data/home/lukemxjia/modelcraft/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-end-user-schema-cleanup/03-RESEARCH.md

<!-- 关键背景：
  - routes.go 第 30 行：import endusergraphql "modelcraft/internal/interfaces/graphql/enduser"
  - routes.go 第 664-706 行：SetupEndUserGraphQLRoutesOnChi 整个函数（含 metaUserResolver 创建代码）
  - chi_setup.go 第 116-121 行：调用块（含 GraphQL Routes - End-User API 注释块）
  - chi_setup.go 第 50 行：架构注释中含 /graphql/end-user/* 描述需更新
  - 注意：SetupEndUserRoutesOnChi（/internal/end-users 管理路由）不删，只删 GraphQL 版本
-->
</context>

<tasks>

<task type="auto">
  <name>任务 1：删除 routes.go 中的 endusergraphql import 和 SetupEndUserGraphQLRoutesOnChi 函数</name>
  <read_first>
    - modelcraft-backend/internal/interfaces/http/routes.go（全文，重点第 25-35 行 import 区和第 660-707 行函数区）
  </read_first>
  <files>modelcraft-backend/internal/interfaces/http/routes.go</files>
  <action>
    在 routes.go 中执行两处删除：

    **删除 1 — import 语句**（第 30 行）：
    删除 `endusergraphql "modelcraft/internal/interfaces/graphql/enduser"` 这一行 import。
    同时检查同一 import 块中是否有 `appEnduser "modelcraft/internal/app/enduser"` 导入——
    该导入仅被 `SetupEndUserGraphQLRoutesOnChi` 内的 `appEnduser.NewPrivateDBManagerAdapter` 和
    `appEnduser.NewMetaUserAppService` 使用，若其他函数没有引用则一并删除；
    若有其他函数引用则保留（执行前 grep 确认）。

    **删除 2 — 整个 SetupEndUserGraphQLRoutesOnChi 函数**（第 664-706 行）：
    删除从注释 `// SetupEndUserGraphQLRoutesOnChi registers End-User GraphQL endpoint.` 到
    最后一个 `}` 的完整代码块（共约 43 行）。
    包括其中创建 endUserResolver、metaUserResolver、internalTokenMW 的代码，
    以及两个 `router.Route(...)` 注册块。
  </action>
  <verify>
    <automated>cd modelcraft-backend && go build ./internal/interfaces/http/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' modelcraft-backend/internal/interfaces/http/routes.go | grep -c 'endusergraphql'` 输出 `0`
    - `grep -c 'SetupEndUserGraphQLRoutesOnChi' modelcraft-backend/internal/interfaces/http/routes.go` 输出 `0`
    - `grep -c 'meta/user' modelcraft-backend/internal/interfaces/http/routes.go` 输出 `0`（meta/user 路由随 end_user 路由组一并删除）
    - `cd modelcraft-backend && go build ./internal/interfaces/http/...` 编译通过，无错误输出
  </acceptance_criteria>
  <done>routes.go 中不再包含 endusergraphql 的任何引用，编译通过</done>
</task>

<task type="auto">
  <name>任务 2：删除 chi_setup.go 中的调用块并更新架构注释</name>
  <read_first>
    - modelcraft-backend/internal/interfaces/http/chi_setup.go（全文，重点第 45-55 行注释区和第 115-125 行调用块）
  </read_first>
  <files>modelcraft-backend/internal/interfaces/http/chi_setup.go</files>
  <action>
    在 chi_setup.go 中执行两处修改：

    **删除 1 — End-User GraphQL 调用块**（第 116-121 行）：
    删除以下代码块（含注释行）：
    ```go
    // ============================================================
    // GraphQL Routes - End-User API
    // ============================================================
    if cfg.DesignHandlers != nil {
        SetupEndUserGraphQLRoutesOnChi(r, cfg.DesignHandlers, cfg.Config)
    }
    ```

    **修改 2 — 架构注释**（第 50 行附近）：
    在文件顶部的架构注释树中找到 `/graphql/end-user/*` 一行，
    删除该描述行（或将其替换为说明已删除的注释），使注释与实际路由结构保持一致。
  </action>
  <verify>
    <automated>cd modelcraft-backend && go build ./internal/interfaces/http/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SetupEndUserGraphQLRoutesOnChi' modelcraft-backend/internal/interfaces/http/chi_setup.go` 输出 `0`
    - `grep -c 'graphql/end-user' modelcraft-backend/internal/interfaces/http/chi_setup.go` 输出 `0`（注释和代码均不含）
    - `cd modelcraft-backend && go build ./internal/interfaces/http/...` 编译通过
  </acceptance_criteria>
  <done>chi_setup.go 中不再调用 SetupEndUserGraphQLRoutesOnChi，架构注释已更新</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 编译期 → 运行期 | 删除 import 和函数后，build 必须通过才能进入 Wave 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | routes.go import 块 | mitigate | 删除后立即 `go build` 验证无残留引用 |
| T-03-02 | Denial of Service | 残留 appEnduser import | accept | 执行前 grep 确认其他函数是否引用，按情况保留或删除 |
</threat_model>

<verification>
Wave 1 整体验证：
```bash
cd modelcraft-backend && go build ./internal/interfaces/http/...
grep -c 'endusergraphql' internal/interfaces/http/routes.go
grep -c 'SetupEndUserGraphQLRoutesOnChi' internal/interfaces/http/chi_setup.go
# meta/user 路由已随 SetupEndUserGraphQLRoutesOnChi 一并删除
grep -c 'meta/user' internal/interfaces/http/routes.go
```
四条命令：第一条编译通过，后三条输出 `0`。
</verification>

<success_criteria>
1. `routes.go` 中 `endusergraphql` import 和 `SetupEndUserGraphQLRoutesOnChi` 函数已删除
2. `chi_setup.go` 中调用块已删除，架构注释已更新
3. `go build ./internal/interfaces/http/...` 通过，无任何编译错误
4. `just lint` 在 http 包范围内无 unused import 警告
5. `grep -c 'meta/user' internal/interfaces/http/routes.go` 输出 0（meta/user 路由随 end_user 路由组一并删除）
</success_criteria>

<output>
完成后创建 `.planning/phases/03-end-user-schema-cleanup/03-01-SUMMARY.md`
</output>
