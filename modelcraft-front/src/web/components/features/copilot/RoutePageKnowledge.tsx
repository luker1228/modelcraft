'use client'

/**
 * RoutePageKnowledge — DEPRECATED.
 * Page-level knowledge has been migrated to the backend agent (agents/tools.py _PAGE_KNOWLEDGE).
 * The backend uses get_page_knowledge(page) tool to load knowledge on demand.
 * This component is kept as an empty shell to avoid breaking any existing imports.
 */
export function RoutePageKnowledge() {
  return null
}
