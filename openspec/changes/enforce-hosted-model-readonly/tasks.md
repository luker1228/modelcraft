## 1. 模型类型落定（创建/导入）

- [x] 1.1 在模型领域对象中明确并统一 `SELF_BUILT` 与 `MANAGED` 类型枚举及映射。
- [x] 1.2 修改创建模型流程：移除外部可变类型输入，后端固定写入 `SELF_BUILT`。
- [x] 1.3 修改导入模型流程：后端固定写入 `MANAGED`，并补充必要校验。

## 2. 托管模型写操作守卫

- [x] 2.1 在应用层新增统一守卫：当模型类型为 `MANAGED` 时拒绝写路径。
- [x] 2.2 接入 DDL 相关入口，确保托管模型 DDL 请求被统一拦截。
- [x] 2.3 接入字段管理入口，确保托管模型字段新增/修改/删除被拦截。
- [x] 2.4 接入数据写入口，确保托管模型新增/更新/删除被拦截。

## 3. 错误语义与接口对齐

- [x] 3.1 增加稳定错误码（如 `MANAGED_MODEL_READONLY`）及统一错误构造。
- [x] 3.2 在 GraphQL/接口层透传可识别错误，保持返回语义一致。
- [ ] 3.3 校验前端依赖的错误字段，确保可用于禁用态与提示文案。

## 4. 前端能力边界收口

- [ ] 4.1 基于模型类型禁用或隐藏托管模型的 DDL 与字段编辑入口。
- [ ] 4.2 在数据页禁用托管模型新增/编辑/删除操作，仅保留查看能力。
- [ ] 4.3 增加托管模型只读提示文案，避免用户误操作。

## 5. 测试与验收

- [x] 5.1 增加创建/导入模型类型归属测试（创建=自建、导入=托管）。
- [x] 5.2 增加托管模型 DDL 拒绝测试。
- [x] 5.3 增加托管模型字段增删改拒绝测试。
- [ ] 5.4 增加托管模型数据写拒绝与数据读成功测试。
- [ ] 5.5 增加自建模型不受影响测试，验证回归安全。